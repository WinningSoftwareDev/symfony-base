framework:
  secret: '%env(APP_SECRET)%'
  session: true
  error_controller: App\Core\Controller\ErrorController::showErrorPage

when@test:
  framework:
    test: true
    session:
      storage_factory_id: session.storage.factory.mock_file
